<Window x:Class="VoidStrike.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:gif="http://wpfanimatedgif.codeplex.com"
        Title="VoidStrike | Login To Continue" Height="450" Width="800"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Background="#000000" CornerRadius="4" BorderBrush="#111111" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Side: GIF Area -->
            <Border Grid.Column="0" Background="#050505" CornerRadius="4,0,0,4" Margin="5" ClipToBounds="True">
                <Grid>
                    <Image gif:ImageBehavior.AnimatedSource="pack://application:,,,/void.gif" Stretch="UniformToFill" Opacity="0.3" />
                    <Border Background="#20000000"/>
                </Grid>
            </Border>

            <!-- Right Side: Login Fields -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="30">
                <TextBlock Text="VOIDSTRIKE" Foreground="#FFFFFF" FontSize="28" FontWeight="Black" Margin="0,0,0,5"/>
                <TextBlock Text="AUTHORIZED ACCESS ONLY" Foreground="#444444" FontSize="10" FontWeight="Bold" Margin="0,0,0,30"/>

                <TextBlock Text="USERNAME" Foreground="#888888" FontSize="10" FontWeight="Black" Margin="0,0,5,5"/>
                <Border Background="#080808" BorderBrush="#222222" BorderThickness="1" CornerRadius="2" Margin="0,0,0,15">
                    <TextBox x:Name="UsernameTxt" Background="Transparent" Foreground="#FFFFFF" BorderThickness="0" Padding="10" FontSize="14" CaretBrush="#FFFFFF"/>
                </Border>

                <TextBlock Text="PASSWORD" Foreground="#888888" FontSize="10" FontWeight="Black" Margin="0,0,5,5"/>
                <Border Background="#080808" BorderBrush="#222222" BorderThickness="1" CornerRadius="2" Margin="0,0,0,30">
                    <PasswordBox x:Name="PasswordTxt" Background="Transparent" Foreground="#FFFFFF" BorderThickness="0" Padding="10" FontSize="14" CaretBrush="#FFFFFF"/>
                </Border>

                <Button Content="ACCESS CORE" Click="Login_Click" Height="45" Background="#FFFFFF" Foreground="Black" FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="2"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button x:Name="DiscordRPCBtn" Content="DISCORD RPC: OFF" Click="DiscordRPC_Toggle" Style="{StaticResource SocialButtonStyle}" Width="150" Margin="5" Background="#111111" Foreground="#5865F2" BorderBrush="#5865F2" BorderThickness="1"/>
                </StackPanel>

                <TextBlock Text="© Mkaf7h Inc" Foreground="#222222" FontSize="10" Margin="0,20,0,0" HorizontalAlignment="Left"/>
                <Button Content="✕" Click="Exit_Click" Background="Transparent" Foreground="#333333" BorderThickness="0" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-240,-10,0" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
